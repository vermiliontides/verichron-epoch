# Minimal Python dependencies for local extractor development and validation.
# Keep this in sync with the actual packages used by the PostgreSQL-backed ETL flow.
#
# Every third-party import anywhere in the repo's Python code (packages/,
# libs/, analysis/, extractors/) must appear here -- there is a single
# repo-root .venv (see libs/runtime-env/runtime_env.py), not one per package,
# so this is the one manifest it installs from. `requests` was imported by
# analysis/automated_forensics.py and analysis/forensics_benchmark.py and was
# missing, so a clean checkout following the README failed at import time on
# the analysis stage only -- after the extractors had already run.

psycopg2-binary>=2.9
pydantic>=2.0
jsonschema>=4.0
requests>=2.31
pytest>=8.0