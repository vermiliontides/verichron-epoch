[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"
 
[project]
name = "verichron-etl-run"
version = "0.1.0"
description = "Shared per-file/per-record success-and-failure accounting for the extractor pipeline (ETLRunResult)."
requires-python = ">=3.10"
dependencies = []
